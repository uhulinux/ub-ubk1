diff -Naur orig/bootStrap.bash patched/bootStrap.bash
--- orig/bootStrap.bash	2016-08-05 02:00:33.000000000 +0200
+++ patched/bootStrap.bash	2016-08-05 02:11:28.863908526 +0200
@@ -50,7 +50,7 @@
         fi
         cd $BUILDDIR 
         cmake $PKG $FAKEROOT $QT_FLAVOR -DCMAKE_EDIT_COMMAND=vim -DCMAKE_INSTALL_PREFIX=/usr $EXTRA $ASAN $DEBUG -G "$BUILDER" $SOURCEDIR || fail cmakeZ
-        make  $PARAL >& /tmp/log$BUILDDIR || fail "make, result in /tmp/log$BUILDDIR"
+        make  $PARAL
 	if  [ "x$PKG" != "x" ] ; then
           $FAKEROOT_COMMAND make package DESTDIR=$FAKEROOT_DIR/tmp || fail package
 	fi
@@ -195,7 +195,7 @@
 
 export TOP=$PWD
 export POSTFIX=""
-export FAKEROOT_DIR=$PWD/install
+export FAKEROOT_DIR=$UB_INSTALLDIR
 export PARAL="$O_PARAL"
 echo "Top dir : $TOP"
 echo "Fake installation directory=$FAKEROOT_DIR"
