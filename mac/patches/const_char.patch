diff -Naur mac-3.99-u4-b5-s7-orig/src/MACLib/APELink.cpp mac-3.99-u4-b5-s7/src/MACLib/APELink.cpp
--- mac-3.99-u4-b5-s7-orig/src/MACLib/APELink.cpp	2010-03-02 16:28:49.000000000 +0100
+++ mac-3.99-u4-b5-s7/src/MACLib/APELink.cpp	2016-04-18 07:18:56.496511883 +0200
@@ -63,10 +63,10 @@
     if (pData != NULL)
     {
         // parse out the information
-        char * pHeader = strstr((char *)pData, APE_LINK_HEADER);
-        char * pImageFile = strstr((char *)pData, APE_LINK_IMAGE_FILE_TAG);
-        char * pStartBlock = strstr((char *)pData, APE_LINK_START_BLOCK_TAG);
-        char * pFinishBlock = strstr((char *)pData, APE_LINK_FINISH_BLOCK_TAG);
+        const char * pHeader = strstr((char *)pData, APE_LINK_HEADER);
+        const char * pImageFile = strstr((char *)pData, APE_LINK_IMAGE_FILE_TAG);
+        const char * pStartBlock = strstr((char *)pData, APE_LINK_START_BLOCK_TAG);
+        const char * pFinishBlock = strstr((char *)pData, APE_LINK_FINISH_BLOCK_TAG);
 
         if (pHeader && pImageFile && pStartBlock && pFinishBlock)
         {
@@ -81,7 +81,7 @@
                 
                 // get the path
                 char cImageFile[MAX_PATH + 1]; int nIndex = 0;
-                char * pImageCharacter = &pImageFile[strlen(APE_LINK_IMAGE_FILE_TAG)];
+                const char * pImageCharacter = &pImageFile[strlen(APE_LINK_IMAGE_FILE_TAG)];
                 while ((*pImageCharacter != 0) && (*pImageCharacter != '\r') && (*pImageCharacter != '\n'))
                     cImageFile[nIndex++] = *pImageCharacter++;
                 cImageFile[nIndex] = 0;
