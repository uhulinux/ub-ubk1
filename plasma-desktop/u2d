dir=$(urllist http://download.kde.org/stable/plasma/ | grep -E '^([0-9])' | sort -V | tail -n 1)
urllist http://download.kde.org/stable/plasma/$dir/ | splitver '^plasma-desktop-([0-9.]+).tar.xz$' | sort -V | tail -n 1
