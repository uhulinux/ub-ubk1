sflist lirc LIRC | grep -E '^([0-9.]+)$' | sort -rV | while read dirver; do
	filever="$(sflist lirc "LIRC/$dirver" | parsever | sort -V | tail -n 1)"
	if [ -n "$filever" ]; then
		echo $filever
		exit
	fi
done
