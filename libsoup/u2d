minor=$(urllist ftp://ftp.gnome.org/pub/GNOME/sources/libsoup/ | splitver '^([0-9.]+)' | sort -V | tail -n 1)
urllist http://ftp.gnome.org/pub/GNOME/sources/libsoup/"$minor" | parsever | sort -V | tail -n 1
