curl -s -k https://raw.githubusercontent.com/siduction/sddm-theme-deepspace/deepspace/debian/changelog | grep deepspace  | sed 's/.*deepspace//g' | sed 's% (%%g' | sed 's%-.*%%g' | sort -V | tail -n 1
