# urllist https://archive.apache.org/dist/openoffice/ | grep -E '^[0-9.]+$' | sort -V | tail -n 1
urllist http://download.ubk.hu/src/misc/aoo/ | splitver '^apache-openoffice-([0-9.].+)-src.tar.bz2$' | sort -V | tail -n 1
