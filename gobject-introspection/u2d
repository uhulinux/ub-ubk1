minor=$(urllist http://ftp.gnome.org/pub/GNOME/sources/gobject-introspection/ | splitver '^([0-9.]+)' | sort -V | tail -n 1)
urllist http://ftp.gnome.org/pub/GNOME/sources/gobject-introspection/"$minor" | parsever | sort -V | tail -n 1
