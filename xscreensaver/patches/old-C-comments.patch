diff -Nur orig/hacks/fontglide.c mod/hacks/fontglide.c
--- orig/hacks/fontglide.c	2018-09-03 00:56:45.000000000 +0200
+++ mod/hacks/fontglide.c	2019-06-06 10:46:43.883104396 +0200
@@ -2421,8 +2421,8 @@
 
   textclient_close (s->tc);
 
-//  if (s->b && s->b != s->window) XFreePixmap (dpy, s->b);
-//  if (s->ba && s->ba != s->b) XFreePixmap (dpy, s->ba);
+/*  if (s->b && s->b != s->window) XFreePixmap (dpy, s->b); */
+/*  if (s->ba && s->ba != s->b) XFreePixmap (dpy, s->ba); */
   XFreeGC (dpy, s->bg_gc);
   if (s->charset) free (s->charset);
   if (s->font_override) free (s->font_override);
